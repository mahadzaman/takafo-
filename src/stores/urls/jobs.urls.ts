import { BaseUrls } from '.';

const JOBS = BaseUrls.JOBS_BASE_URL + '/all-jobs';

const RAP = BaseUrls.JOBS_BASE_URL + '/rap';

const JOBS_DETAILS = BaseUrls.JOBS_BASE_URL + '/details';

const REQUESTED_JOBS = BaseUrls.JOBS_BASE_URL + '/requested-jobs';

const APPROVED_JOBS = BaseUrls.JOBS_BASE_URL + '/approved-jobs';

const REJECTED_JOBS = BaseUrls.JOBS_BASE_URL + '/rejected-jobs';

const ASSOCIATED_JOBS = BaseUrls.JOBS_BASE_URL + '/associated-jobs';

const ADVERT = BaseUrls.JOBS_BASE_URL + '/advert/by-job-id';

const QUESTIONS = BaseUrls.JOBS_BASE_URL + '/questions';

export default {
  JOBS,
  JOBS_DETAILS,
  REQUESTED_JOBS,
  APPROVED_JOBS,
  REJECTED_JOBS,
  ASSOCIATED_JOBS,
  ADVERT,
  RAP,
  QUESTIONS
};
